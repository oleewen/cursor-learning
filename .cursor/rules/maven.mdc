---
description: Maven依赖和版本管理规范
globs: */pom.xml
alwaysApply: false
---

# 版本管理
- 使用Maven管理依赖和构建程序
- 工程规范中的api和common模块需要对外开放，不依赖于工程的parent pom，独立管理自己的版本号，依赖的包指定版本号
- 其他模块保持跟parent一样的版本，保持SNAPSHOT版本，并采用maven的revision统一版本号
- 继承工程parent的模块，依赖的包不指定版本号，继承parent的版本号